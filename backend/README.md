Server side using Apollo Server, that interacts with the api of thingiverse.

## Config

To properly interact with the api, you will need to get a valid token following the [thingiverse getting
started](https://www.thingiverse.com/developers/getting-started) and set it to the variable `ACCESS_TOKEN` in the console you're going to boot the server.